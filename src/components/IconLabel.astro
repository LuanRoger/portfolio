---
interface Props {
  className?: string;
  label: string;
}

const { label, className } = Astro.props;
---

<div class:list={["flex gap-2 items-center", className]}>
  <slot />
  <span>{label}</span>
</div>
